spring:
  application:
    name: order-api

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin
    dynamic: true

logging:
  level:
    org.springframework.amqp: DEBUG